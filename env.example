# Environment Configuration
# Copy this file to .env and fill in your actual values

# Application Environment (development, staging, production)
ENVIRONMENT=development

# Server Port (default: 8080)
PORT=8080

# Gin Mode (debug, release, test - auto-detected based on ENVIRONMENT if not set)
# GIN_MODE=debug

# JWT Configuration
# REQUIRED: Generate a secure random string for JWT signing
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Token Lifetime (default: 24h)
# Examples: 1h, 24h, 7d, 30d
TOKEN_LIFETIME=24h

# CORS Configuration
# Comma-separated list of allowed origins (leave empty to allow all in development)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Static Files Configuration
# STATIC_PATH=./uploads
# STATIC_URL=/static
# UPLOADS_PATH=./uploads

# API Configuration
# API_PREFIX=/api

# Database Configuration
# Option 1: Use full DATABASE_URL (recommended for production)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name?sslmode=disable

# Option 2: Use individual database parameters
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=your_database_user
# DB_PASSWORD=your_database_password
# DB_NAME=your_database_name
# DB_SSLMODE=disable

# Database Connection Pool (optional)
# DB_MAX_OPEN_CONNS=25
# DB_MAX_IDLE_CONNS=5

# Environment File Path (optional, defaults to .env)
# ENV_FILE=.env
